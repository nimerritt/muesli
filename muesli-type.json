{
  "id": "https://raw.githubusercontent.com/nimerritt/muesli/master/muesli-type.json",
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Core schema meta-schema",
  "definitions": {
      "muesliType": {
        "oneOf": [
          { "$ref": "#/definitions/objectType" },
          { "$ref": "#/definitions/arrayType" },
          { "$ref": "#/definitions/numberType" },
          { "$ref": "#/definitions/booleanType" },
          { "$ref": "#/definitions/stringType" }
        ]
      },
      "objectType": {
        "type": "object",
        "properties": {
          "type": { "type": "string", "enum": ["object"] },
          "properties": { "type": "object", "additionalProperties": { "$ref": "#/definitions/muesliType" } },
          "requiredProperties": { "type": "array", "items": { "type": "string" } },
          "additionalProperties": { "type": "boolean" }
        },
        "additionalProperties": false,
        "requiredProperties": ["type", "properties", "requiredProperties", "additionalProperties"]
      },
      "arrayType": {
        "type": "object",
        "properties": {
          "type": { "type": "string", "enum": ["array"] },
          "items": { "$ref": "#/definitions/muesliType" }
        },
        "additionalProperties": false,
        "requiredProperties": ["type", "items"]
      },
      "numberType": {
        "type": "object",
        "properties": {
          "type": { "type": "string", "enum": ["number"] }
        },
        "additionalProperties": false,
        "requiredProperties": ["type"]
      },
      "stringType": {
        "type": "object",
        "properties": {
          "type": { "type": "string", "enum": ["string"] }
        },
        "additionalProperties": false,
        "requiredProperties": ["type"]
      },
      "booleanType": {
        "type": "object",
        "properties": {
          "type": { "type": "string", "enum": ["boolean"] }
        },
        "additionalProperties": false,
        "requiredProperties": ["type"]
      }
  },
  "type": "object",
  "properties": {
    "$schema": { "type": "string", "format": "uri" },
    "title": { "type": "string" },
    "type": { "type": "string", "enum": ["object"] },
    "properties": { "type": "object", "additionalProperties": { "$ref": "#/definitions/muesliType" } },
    "requiredProperties": { "type": "array", "items": { "type": "string" } },
    "additionalProperties": { "type": "boolean" }
  },
  "additionalProperties": false,
  "requiredProperties": ["title", "type", "properties", "requiredProperties", "additionalProperties"]
}

